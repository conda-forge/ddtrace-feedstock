{% set name = "ddtrace" %}
{% set version = "0.45.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ddtrace-{{ version }}.tar.gz
  sha256: d007d29272ee003d4f82e2dae6ef3637ed222dec71ffcdcf5ce820bb9c97af4e

build:
  number: 0
  skip: true  # [py==30 or py==31 or py==32 or py==33 or py==34]
  entry_points:
    console_scripts:
      - ddtrace-run = ddtrace.commands.ddtrace_run:main
      - pyddprofile = ddtrace.profiling.__main__:main
    pytest11:
      - ddtrace = ddtrace.contrib.pytest.plugin
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython
    - python
    - pip
    - setuptools_scm >=4
  run:
    - python
    - pip
    - enum34  # [py<34]
    - funcsigs >=1.0.0  # [py==27]
    - typing # [py<35]
    - intervaltree
    - protobuf >=3
    - tenacity >=5

test:
  imports:
    - ddtrace
    - ddtrace.bootstrap
  requires:
    - tox
    - flake8

about:
  home: https://github.com/DataDog/dd-trace-py
  summary: Datadog tracing code
  license: BSD-3-Clause
  license_file: LICENSE.BSD3

extra:
  recipe-maintainers:
    - aymondebroglie
    - SarvarD
